# Python 3.12. Direct dependencies only, pinned to the versions that resolved
# cleanly on 2026-09-19. Transitive deps are left to the resolver -- pinning all
# ~130 of them would make this file unreadable, and readability wins here.
# Regenerate a full lock with `pip freeze` if a deploy ever needs one.

# Graph orchestration
langgraph==1.2.11
langchain-openai==1.6.2

# Embeddings + generation
openai==3.16.2

# Vector store
chromadb==1.5.9

# Lexical retrieval (hybrid with the vector store)
rank-bm25==0.2.2

# Corpus acquisition and parsing
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.3

# UI
streamlit==1.64.0

# Config and evals
python-dotenv==1.2.3
pyyaml==6.0.3

# Tests
pytest==9.1.1
