[pytest]
# The tests import `core.*` from the project root. Bare `pytest` does not put
# the root on sys.path (only `python -m pytest` does), so CLAUDE.md's documented
# `pytest -q` failed with ModuleNotFoundError until this was added.
pythonpath = .
testpaths = tests
